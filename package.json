{
  "name": "detector-app",
  "version": "1.0.0",
  "description": "Face detection app frontend tests",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}

